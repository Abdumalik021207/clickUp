package org.example.clickup.repository;

import org.example.clickup.model.Time_tracker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Time_trackerRepo extends JpaRepository<Time_tracker, Long> {
}
