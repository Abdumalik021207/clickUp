package org.example.clickup.repository;

import org.example.clickup.model.Task_dependency;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_dependencyRepo extends JpaRepository<Task_dependency, Long> {
}
