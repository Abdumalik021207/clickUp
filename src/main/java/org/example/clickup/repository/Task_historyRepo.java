package org.example.clickup.repository;

import org.example.clickup.model.Task_history;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_historyRepo extends JpaRepository<Task_history, Long> {
}
