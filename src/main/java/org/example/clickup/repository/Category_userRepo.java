package org.example.clickup.repository;

import org.example.clickup.model.Category_user;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Category_userRepo extends JpaRepository<Category_user, Long> {
}
