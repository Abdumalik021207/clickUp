package org.example.clickup.repository;

import org.example.clickup.model.Project_user;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Project_userRepo extends JpaRepository<Project_user, Integer> {
}
