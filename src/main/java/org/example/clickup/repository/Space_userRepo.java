package org.example.clickup.repository;

import org.example.clickup.model.Space_user;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Space_userRepo extends JpaRepository<Space_user, Long> {
}
