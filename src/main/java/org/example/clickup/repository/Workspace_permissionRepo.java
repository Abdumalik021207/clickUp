package org.example.clickup.repository;

import org.example.clickup.model.Workspace_permission;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Workspace_permissionRepo extends JpaRepository<Workspace_permission, Integer> {
}
