package org.example.clickup.repository;

import org.example.clickup.model.Task_tag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_tagRepo extends JpaRepository<Task_tag, Long> {
}
