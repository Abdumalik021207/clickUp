package org.example.clickup.repository;

import org.example.clickup.model.Workspace_role;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Workspace_roleRepo extends JpaRepository<Workspace_role, Integer> {
}
