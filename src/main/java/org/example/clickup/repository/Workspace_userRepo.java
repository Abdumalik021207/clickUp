package org.example.clickup.repository;

import org.example.clickup.model.Workspace_user;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Workspace_userRepo extends JpaRepository<Workspace_user, Long> {
}
